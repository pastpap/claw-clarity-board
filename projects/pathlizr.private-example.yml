# private-example only
#
# Pathlizr belongs in private mode, not public mode.
# Keep real internal project state in a private data source and point the board to it with:
# CLARITY_DATA_DIR=/path/to/private/projects
